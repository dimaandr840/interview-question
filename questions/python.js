export default {
  professionSlug: "python",
  categories: [
    { slug: "python-basics", title: "Основы Python", emoji: "", description: "Синтаксис, типы данных и базовые механизмы языка." },
    { slug: "collections", title: "Структуры данных", emoji: "", description: "Списки, словари, множества и кортежи." },
    { slug: "oop", title: "ООП и модель объектов", emoji: "", description: "Классы, наследование, MRO и dataclass." },
    { slug: "functions", title: "Функции и декораторы", emoji: "", description: "Замыкания, генераторы, итераторы и декораторы." },
    { slug: "memory", title: "Память и управление ресурсами", emoji: "", description: "Ссылки, GIL, сборка мусора и контекстные менеджеры." },
    { slug: "exceptions", title: "Исключения и ошибки", emoji: "", description: "Обработка и проектирование ошибок." },
    { slug: "concurrency", title: "Конкурентность и asyncio", emoji: "", description: "Потоки, процессы, event loop и синхронизация." },
    { slug: "io", title: "Стандартная библиотека и IO", emoji: "", description: "Файлы, pathlib и средства стандартной библиотеки." },
    { slug: "testing", title: "Тестирование", emoji: "", description: "pytest, unittest, фикстуры и mock." },
    { slug: "databases", title: "Базы данных и ORM", emoji: "", description: "SQL, транзакции, ORM и индексы." },
    { slug: "web", title: "Веб-разработка", emoji: "", description: "HTTP, REST, Django, FastAPI и Flask." },
    { slug: "algorithms", title: "Алгоритмы и задачи", emoji: "", description: "Сложность, поиск, сортировка и типовые задачи." },
    { slug: "architecture", title: "Архитектура и чистый код", emoji: "", description: "SOLID, паттерны, зависимости и модульность." },
    { slug: "devops", title: "DevOps и развёртывание", emoji: "", description: "Docker, CI/CD, конфигурация и секреты." }
  ],
  questions: [
    {
      t: "Чем отличаются списки, кортежи и множества в Python?", l: "Junior", c: "python-basics",
      g: ["python", "collections", "data-structures"],
      d: "Список хранит упорядоченные изменяемые элементы, кортеж хранит упорядоченные неизменяемые элементы, а множество содержит уникальные хешируемые значения. Выбор зависит от порядка, изменяемости и проверки принадлежности.", pop: true,
      s: [
        { h: "Короткий ответ", p: ["<code>list</code> изменяем и сохраняет порядок, <code>tuple</code> неизменяем и упорядочен, а <code>set</code> хранит уникальные хешируемые элементы без индексного доступа."] },
        { h: "Как это работает подробнее", p: ["Список представляет изменяемую последовательность ссылок на объекты. При добавлении элемент помещается в массив ссылок, а при нехватке ёмкости Python выделяет новый участок памяти и переносит ссылки. Кортеж использует последовательное хранение, но его набор ссылок фиксируется после создания. Это позволяет безопасно передавать запись, применять кортеж как часть ключа и не опасаться изменения самой структуры. Множество основано на хеш-таблице: для каждого элемента вычисляется хеш, затем выбирается позиция с учётом коллизий. Поэтому проверка принадлежности обычно имеет амортизированную сложность O(1), но требует хешируемых элементов. Порядок множества не является контрактом интерфейса. Все три типа могут содержать объекты разных классов, потому что контейнеры хранят ссылки, а не копии значений. Выбор структуры должен следовать смыслу данных и требуемым операциям."] , b: ["<strong>Порядок:</strong> индекс нужен для списка или кортежа, а множеству важнее уникальность и быстрая проверка входа."] },
        { h: "Пример кода", code: { lang: "python", title: "collections_demo.py", lines: ["items = [\"python\", \"python\", \"go\"]", "point = (10, 20)", "unique_items = set(items)", "items.append(\"rust\")", "print(items)", "print(point[0])", "print(unique_items)"] } },
        { h: "Что использовать на практике", b: ["Используйте список для изменяемой последовательности, кортеж для фиксированной записи, множество для удаления дублей и быстрой проверки принадлежности."] },
        { h: "Подводные камни", b: ["<mark>Не путайте неизменяемый кортеж с глубокой неизменяемостью:</mark> вложенный список внутри кортежа всё ещё можно изменить.", "Не используйте порядок множества как часть формата вывода или сериализации."] },
        { h: "Как отвечать на собеседовании", b: ["Изменяемость и порядок → хешируемость и уникальность → подходящие операции → выбор структуры для конкретной задачи."] }
      ]
    },
    {
      t: "Что такое изменяемые и неизменяемые объекты в Python?", l: "Junior", c: "python-basics",
      g: ["python", "objects", "memory"],
      d: "Изменяемый объект можно изменить после создания, сохранив его идентичность, а неизменяемый изменить на месте нельзя. Разница влияет на ссылки, аргументы функций, копирование и возможность использовать объект как ключ словаря.", pop: true,
      s: [
        { h: "Короткий ответ", p: ["<code>list</code>, <code>dict</code> и <code>set</code> изменяемы, а <code>int</code>, <code>str</code> и обычный <code>tuple</code> неизменяемы. Операция над неизменяемым значением создаёт новый объект."] },
        { h: "Как это работает подробнее", p: ["Имя в Python связано с объектом, а не содержит его значение непосредственно. При присваивании <code>b = a</code> создаётся ещё одна ссылка на тот же объект. Если объект изменяемый, например список, вызов <code>append</code> меняет его на месте, и результат виден через обе ссылки. Для строки или числа операция, похожая на изменение, создаёт новый объект и переназначает имя. Идентичность можно наблюдать через <code>id</code>, а равенство проверяет содержимое. Хешируемость обычно требует неизменяемого состояния, поэтому строка и число могут быть ключами словаря, а список нет. Аргументы передаются как ссылки на объекты, поэтому функция может изменить переданный контейнер, хотя не может заменить объект в области вызывающего имени. Копирование нужно выбирать явно: поверхностная копия отделяет внешний контейнер, но не вложенные ссылки. Неизменяемость упрощает совместное использование данных, но не делает автоматически неизменяемыми вложенные объекты."] , b: ["<strong>Ссылка:</strong> присваивание не копирует объект, поэтому изменение контейнера может быть видно из нескольких имён."] },
        { h: "Пример кода", code: { lang: "python", title: "mutability_demo.py", lines: ["values = [1, 2]", "alias = values", "alias.append(3)", "print(values)", "name = \"Ann\"", "old_id = id(name)", "name += \" Lee\"", "print(old_id == id(name))"] } },
        { h: "Что использовать на практике", b: ["Делайте явную копию перед передачей коллекции, если вызываемый код не должен менять исходные данные. Для ключей и настроек выбирайте стабильные неизменяемые значения."] },
        { h: "Подводные камни", b: ["<mark>Изменяемый аргумент по умолчанию</mark> создаётся один раз и может сохранять состояние между вызовами.", "Не путайте неизменяемость контейнера с неизменяемостью его содержимого."] },
        { h: "Как отвечать на собеседовании", b: ["Ссылка на объект → изменение на месте или создание нового → последствия для аргументов, копирования и ключей словаря."] }
      ]
    },
    {
      t: "Как работает поиск имён по правилу LEGB в Python?", l: "Junior", c: "python-basics", g: ["python", "scope", "functions"],
      d: "Python ищет имя в порядке LEGB: локальная область, область внешней функции, глобальная область модуля и встроенные имена. Первый найденный объект используется, поэтому область видимости определяет поведение кода.",
      s: [
        { h: "Короткий ответ", p: ["LEGB означает <code>Local</code>, <code>Enclosing</code>, <code>Global</code>, <code>Built-in</code>. Поиск идёт от ближайшей области к дальней и останавливается на первом найденном имени."] },
        { h: "Как это работает подробнее", p: ["При компиляции функции Python анализирует присваивания и определяет, какие имена считаются локальными. Во время выполнения обращение к имени сначала проверяет локальные значения текущего кадра. Во вложенной функции затем используется область внешней функции, если имя захвачено замыканием. Следующей проверяется таблица глобальных имён модуля, а затем пространство встроенных объектов, где находятся <code>len</code>, <code>print</code> и другие функции. Локальное имя скрывает внешнее с тем же названием, поэтому чрезмерное переиспользование имён затрудняет чтение. Оператор <code>global</code> сообщает, что присваивание относится к модулю, а <code>nonlocal</code> разрешает менять переменную ближайшей внешней функции. Эти операторы не меняют порядок поиска для всех имён, они влияют прежде всего на область присваивания. Если локальная переменная читается до присваивания, возникает <code>UnboundLocalError</code>. Явная передача зависимостей через параметры обычно делает код проще для тестирования и повторного использования."] , b: ["<strong>Разрешение:</strong> сначала проверяется ближайшая область, а <code>global</code> и <code>nonlocal</code> меняют цель присваивания."] },
        { h: "Пример кода", code: { lang: "python", title: "legb_demo.py", lines: ["value = \"global\"", "", "def outer():", "    value = \"enclosing\"", "", "    def inner():", "        print(value)", "", "    inner()", "", "outer()"] } },
        { h: "Что использовать на практике", b: ["Ограничивайте состояние функциями, передавайте зависимости параметрами и оставляйте глобальные значения только для действительно общих неизменяемых настроек."] },
        { h: "Подводные камни", b: ["Присваивание внутри функции делает имя локальным во всей функции, даже если чтение стоит раньше присваивания.", "Не называйте собственную переменную именем встроенной функции без необходимости."] },
        { h: "Как отвечать на собеседовании", b: ["Local → Enclosing → Global → Built-in → роль <code>global</code> и <code>nonlocal</code> в присваивании и замыканиях."] }
      ]
    },
    {
      t: "Что делают *args и **kwargs в Python?", l: "Junior", c: "python-basics", g: ["python", "functions", "arguments"],
      d: "Параметр *args собирает произвольные позиционные аргументы в кортеж, а **kwargs собирает именованные аргументы в словарь. Они помогают строить адаптеры и обёртки, которые гибко передают параметры дальше.",
      s: [
        { h: "Короткий ответ", p: ["<code>*args</code> собирает дополнительные позиционные аргументы в кортеж, а <code>**kwargs</code> собирает дополнительные именованные аргументы в словарь. При вызове эти формы выполняют обратную распаковку."] },
        { h: "Как это работает подробнее", p: ["Звёздочные формы имеют два связанных применения. В сигнатуре функции <code>*args</code> принимает все лишние позиционные значения в новый кортеж, а <code>**kwargs</code> собирает именованные значения в словарь. При вызове функции <code>*</code> извлекает элементы из итерируемого объекта, а <code>**</code> передаёт пары ключ-значение из отображения как именованные аргументы. Явные параметры лучше ставить отдельно, если они входят в стабильный контракт. После <code>*args</code> можно объявить ключевые параметры, которые вызывающий код обязан передать по имени. Для словарной распаковки ключи должны быть строками и допустимыми именами параметров, если их принимает обычная функция. Внутри декоратора эти конструкции позволяют сохранить универсальную сигнатуру, но их полезно сочетать с <code>functools.wraps</code> и ясной документацией. Избыточная гибкость ухудшает проверку входных данных: ошибка появляется глубже в коде и становится менее понятной. Поэтому собирайте только те аргументы, которые действительно нужны адаптеру, делегированию или расширяемому интерфейсу."] , b: ["<strong>Сбор и распаковка:</strong> в определении звёздочки собирают значения, а при вызове распаковывают последовательность или словарь."] },
        { h: "Пример кода", code: { lang: "python", title: "arguments_demo.py", lines: ["def describe(title, *tags, **options):", "    print(title)", "    print(tags)", "    print(options)", "", "tags = [\"python\", \"backend\"]", "options = {\"level\": \"junior\"}", "describe(\"Курс\", *tags, **options)"] } },
        { h: "Что использовать на практике", b: ["Применяйте формы в декораторах, адаптерах и делегирующих функциях. Обязательные параметры публичного API объявляйте явно, чтобы контракт был понятен."] },
        { h: "Подводные камни", b: ["Не используйте <code>**kwargs</code> вместо валидации. Неявный набор параметров усложняет документацию, типизацию и поиск ошибок.", "Следите за конфликтами между явным параметром и тем же ключом в распакованном словаре."] },
        { h: "Как отвечать на собеседовании", b: ["Сбор аргументов в кортеж и словарь → обратная распаковка при вызове → контроль контракта и оправданные случаи гибкости."] }
      ]
    },
    {
      t: "В чём разница между поверхностной и глубокой копией в Python?", l: "Middle", c: "python-basics", g: ["python", "copying", "nested-data"],
      d: "Поверхностная копия создаёт новый внешний контейнер, но сохраняет ссылки на вложенные объекты. Глубокая копия рекурсивно копирует структуру и дороже по времени и памяти, поэтому не всегда является лучшим решением.",
      s: [
        { h: "Короткий ответ", p: ["<code>copy.copy</code> копирует внешний объект, а <code>copy.deepcopy</code> рекурсивно создаёт копии вложенных объектов, формируя независимую структуру."] },
        { h: "Как это работает подробнее", p: ["Поверхностная копия создаёт новый объект верхнего уровня. Например, после копирования списка добавление элемента в один список не меняет другой, но вложенный список внутри них остаётся общей ссылкой. Поэтому изменение вложенного контейнера видно с обеих сторон. Глубокая копия проходит по графу объектов рекурсивно и создаёт новые вложенные значения. Для циклических ссылок модуль <code>copy</code> ведёт таблицу уже обработанных объектов, чтобы не уйти в бесконечную рекурсию и сохранить повторное совместное использование. Такой процесс может быть дорогим по памяти и времени. Он также не способен корректно скопировать любой внешний ресурс: файл, сетевое соединение, блокировку или объект расширения. Некоторые классы определяют собственную логику копирования через специальные методы. Часто явное построение нового доменного объекта безопаснее, потому что позволяет выбрать поля и намеренно сохранить общие зависимости. Срез списка и метод <code>list.copy()</code> делают только поверхностную копию. Перед выбором нужно определить, какие части состояния должны быть независимыми, а какие ссылки являются общими по смыслу."] , b: ["<strong>Граница копии:</strong> сначала решите, должны ли вложенные объекты разделяться, и только потом выбирайте поверхностную или глубокую копию."] },
        { h: "Пример кода", code: { lang: "python", title: "copy_demo.py", lines: ["from copy import copy, deepcopy", "", "source = [[1, 2], [3, 4]]", "shallow = copy(source)", "deep = deepcopy(source)", "source[0].append(5)", "print(shallow)", "print(deep)"] } },
        { h: "Что использовать на практике", b: ["Используйте поверхностную копию для независимого внешнего контейнера. Глубокую применяйте только при доказанной необходимости полной независимости вложенного графа."] },
        { h: "Подводные камни", b: ["<mark>Глубокая копия не универсальна:</mark> объекты с файлами, соединениями и блокировками могут копироваться некорректно.", "Срез и <code>list.copy()</code> не копируют вложенные элементы."] },
        { h: "Как отвечать на собеседовании", b: ["Новый внешний контейнер → общие или независимые вложенные объекты → стоимость операции → явное построение сложной модели как альтернатива."] }
      ]
    },
    {
      t: "Как работают генераторы и yield в Python?", l: "Middle", c: "python-basics", g: ["python", "generators", "iterators"],
      d: "Генератор выдаёт значения лениво через yield и сохраняет состояние между шагами. Он позволяет обрабатывать большие последовательности и потоки данных без хранения всех результатов одновременно.",
      s: [
        { h: "Короткий ответ", p: ["Функция с <code>yield</code> возвращает генератор. Каждый вызов <code>next()</code> продолжает выполнение до следующего <code>yield</code>, а после окончания возникает <code>StopIteration</code>."] },
        { h: "Как это работает подробнее", p: ["Наличие <code>yield</code> меняет функцию в генераторную: обычный вызов не выполняет тело целиком, а возвращает объект-итератор. При первом <code>next</code> выполнение начинается и останавливается на первом операторе <code>yield</code>. Выданное значение передаётся вызывающему коду, а локальные переменные, текущая инструкция и состояние стека сохраняются внутри генератора. Следующий вызов продолжает выполнение с этого места. Значение, переданное в <code>send</code>, может стать результатом выражения <code>yield</code>, поэтому генератор способен принимать данные, хотя чаще он используется как источник. При достижении конца функция завершается, а цикл получает <code>StopIteration</code>. Генератор хранит только текущую позицию и необходимые ссылки, поэтому хорошо подходит для потоковой обработки файлов, сетевых ответов и конвейеров преобразований. Цена ленивости состоит в одноразовости и отсутствии случайного доступа. Ошибки выполняются не при создании генератора, а при его обходе, что важно учитывать в тестах и обработке исключений. Если данные нужны несколько раз, лучше материализовать результат или создать новую генераторную последовательность."] , b: ["<strong>Ленивость:</strong> вычисление происходит при обходе, поэтому память экономится, но побочные эффекты и ошибки отложены до вызова итератора."] },
        { h: "Пример кода", code: { lang: "python", title: "generator_demo.py", lines: ["def batches(items, size):", "    for start in range(0, len(items), size):", "        yield items[start:start + size]", "", "for batch in batches([1, 2, 3, 4, 5], 2):", "    print(batch)"] } },
        { h: "Что использовать на практике", b: ["Используйте генераторы для больших файлов, пакетной обработки, ленивых фильтров и конвейеров, когда все результаты не нужны одновременно."] },
        { h: "Подводные камни", b: ["<mark>Генератор одноразовый:</mark> после исчерпания повторный обход не вернёт значения.", "Не выбирайте его для многократного доступа по индексу или повторяемого обхода без пересоздания."] },
        { h: "Как отвечать на собеседовании", b: ["Ленивое вычисление → сохранение состояния на <code>yield</code> → экономия памяти → одноразовость и отложенное выполнение."] }
      ]
    },
    {
      t: "Что такое декораторы функций в Python?", l: "Middle", c: "python-basics", g: ["python", "decorators", "higher-order-functions"],
      d: "Декоратор принимает функцию и возвращает новую вызываемую функцию с дополнительным поведением. Он помогает добавлять логирование, проверку доступа, кэширование и измерение времени без переписывания основного тела.",
      s: [
        { h: "Короткий ответ", p: ["Декоратор оборачивает вызываемый объект и добавляет поведение до, после или вместо вызова исходной функции. Синтаксис <code>@name</code> применяет его при создании функции."] },
        { h: "Как это работает подробнее", p: ["Функции в Python являются объектами: их можно передавать как аргументы, сохранять в переменных и возвращать из других функций. Декоратор получает исходную функцию и обычно создаёт замыкание-обёртку. Обёртка принимает параметры, выполняет дополнительную логику, вызывает оригинал и возвращает его результат. Запись <code>@logged</code> над определением эквивалентна присваиванию результата <code>logged(function)</code> после создания функции. Если декоратору нужны настройки, появляется дополнительный внешний вызов, который сначала принимает конфигурацию, затем возвращает настоящий декоратор. <code>functools.wraps</code> переносит имя, документацию и другие метаданные, чтобы интроспекция и трассировка не указывали только на обёртку. При нескольких декораторах важен порядок: ближайший к функции применяется первым, а внешний выполняется при вызове раньше или позже в соответствии со слоями. Декораторы удобны для сквозных аспектов, но скрытая логика может усложнить отладку. Их следует применять к стабильным правилам и сохранять предсказуемую сигнатуру через типизацию и документацию."] , b: ["<strong>Обёртка:</strong> функция получает вызов, выполняет сквозную логику и делегирует работу оригиналу, а <code>wraps</code> сохраняет его метаданные."] },
        { h: "Пример кода", code: { lang: "python", title: "decorator_demo.py", lines: ["from functools import wraps", "", "def logged(func):", "    @wraps(func)", "    def wrapper(*args, **kwargs):", "        print(\"Вызов функции\")", "        return func(*args, **kwargs)", "    return wrapper", "", "@logged", "def add(a, b):", "    return a + b", "", "print(add(2, 3))"] } },
        { h: "Что использовать на практике", b: ["Выносите в декораторы аудит, авторизацию, кэширование, повторы и метрики. Основной код функции оставляйте предметным и коротким."] },
        { h: "Подводные камни", b: ["Без <code>wraps</code> ухудшаются интроспекция и документация.", "Длинная цепочка декораторов скрывает порядок выполнения и усложняет отладку."] },
        { h: "Как отвечать на собеседовании", b: ["Функции как объекты → обёртка и синтаксис <code>@</code> → <code>wraps</code> → порядок нескольких декораторов и практические случаи."] }
      ]
    },
    {
      t: "Как работает контекстный менеджер with в Python?", l: "Middle", c: "python-basics", g: ["python", "context-managers", "resources"],
      d: "Контекстный менеджер управляет входом в блок и гарантирует освобождение ресурса при выходе, включая выход из-за исключения. Конструкция with вызывает __enter__ при входе и __exit__ при завершении.",
      s: [
        { h: "Короткий ответ", p: ["<code>with</code> вызывает <code>__enter__</code> при входе и <code>__exit__</code> при выходе, поэтому ресурс можно настроить и освободить в одном управляемом блоке."] },
        { h: "Как это работает подробнее", p: ["Контекстный менеджер задаёт протокол жизненного цикла ресурса. Перед выполнением тела блока Python получает менеджер и вызывает его <code>__enter__</code>. Возвращённое значение можно связать с именем после <code>as</code>. Затем выполняется тело. При обычном завершении или при исключении вызывается <code>__exit__</code>. Ей передаются тип исключения, само значение и объект трассировки, либо три пустых значения при нормальном выходе. Если <code>__exit__</code> возвращает истинное значение, исключение подавляется, поэтому такой возврат должен быть осознанной частью контракта. Файловый объект закрывается через этот механизм, а транзакционный менеджер может подтвердить или откатить изменения. Конструкция полезна и для блокировок, временных каталогов, сетевых соединений и тестовых ресурсов. Декоратор <code>contextlib.contextmanager</code> позволяет выразить протокол функцией с одним <code>yield</code>: код до него выполняет вход, код после него выполняет выход. Важен принцип: очистка должна быть безопасной даже при ошибке в пользовательском блоке. Если освобождение само может завершиться исключением, нужно определить, как сохранить исходную причину и не скрыть повреждение операции."] , b: ["<strong>Выход:</strong> <code>__exit__</code> вызывается при любом завершении блока, а истинный результат подавляет исключение и требует явной политики."] },
        { h: "Пример кода", code: { lang: "python", title: "context_manager_demo.py", lines: ["class Resource:", "    def __enter__(self):", "        print(\"Открытие\")", "        return self", "", "    def __exit__(self, exc_type, exc_value, traceback):", "        print(\"Закрытие\")", "        return False", "", "with Resource():", "    print(\"Работа\")"] } },
        { h: "Что использовать на практике", b: ["Используйте <code>with</code> для файлов, транзакций, соединений, блокировок и временных ресурсов, чтобы жизненный цикл был явным и надёжным."] },
        { h: "Подводные камни", b: ["Не возвращайте истинное значение из <code>__exit__</code> без осознанной политики подавления ошибок.", "Освобождение ресурса должно происходить и после исключения внутри блока."] },
        { h: "Как отвечать на собеседовании", b: ["<code>__enter__</code> при входе → тело блока → <code>__exit__</code> при любом выходе → безопасное управление ресурсом."] }
      ]
    },
    {
      t: "Как устроен протокол итерации в Python?", l: "Senior", c: "python-basics", g: ["python", "iteration", "protocols"],
      d: "Итерируемый объект возвращает итератор через __iter__, а итератор выдаёт элементы через __next__. Цикл for вызывает эти методы и завершает обход, когда получает StopIteration.",
      s: [
        { h: "Короткий ответ", p: ["Итерируемый объект поддерживает <code>__iter__</code>, а итератор поддерживает <code>__iter__</code> и <code>__next__</code>. Конец обхода обозначается <code>StopIteration</code>."] },
        { h: "Как это работает подробнее", p: ["Цикл <code>for</code> не знает внутреннее устройство контейнера. Сначала он вызывает <code>iter(obj)</code> и получает объект итератора. Затем цикл повторяет вызов <code>next(iterator)</code>, присваивает результат переменной и выполняет тело. Когда элементов больше нет, итератор возбуждает <code>StopIteration</code>, а сам цикл перехватывает это исключение и завершает обход без ошибки для вызывающего кода. Итерируемый контейнер обычно возвращает новый итератор при каждом вызове <code>__iter__</code>, поэтому его можно обходить повторно и независимо. Сам итератор часто хранит позицию и возвращает себя из <code>__iter__</code>, поэтому после исчерпания повторный обход невозможен. Генератор является таким одноразовым итератором. Собственный класс итератора должен явно определить состояние, условие окончания и значение следующего элемента. Для сложного или ленивого линейного обхода генератор обычно проще, а класс полезен, когда протокол является частью публичной модели. Нельзя рассчитывать на случайный доступ, если объект предоставляет только итерацию. В генераторах для завершения лучше использовать обычный <code>return</code>, поскольку явный <code>StopIteration</code> внутри может превратиться в ошибку."] , b: ["<strong>Контракт:</strong> <code>iter</code> получает итератор, <code>next</code> выдаёт значение, а <code>StopIteration</code> обозначает конец последовательности."] },
        { h: "Пример кода", code: { lang: "python", title: "iterator_demo.py", lines: ["class Countdown:", "    def __init__(self, start):", "        self.current = start", "", "    def __iter__(self):", "        return self", "", "    def __next__(self):", "        if self.current == 0:", "            raise StopIteration", "        value = self.current", "        self.current -= 1", "        return value", "", "for value in Countdown(3):", "    print(value)"] } },
        { h: "Что использовать на практике", b: ["Для простого ленивого обхода используйте генератор. Собственный итератор пишите, когда нужно контролировать состояние, протокол или необычное завершение."] },
        { h: "Подводные камни", b: ["Возврат <code>self</code> из <code>__iter__</code> делает объект одноразовым, что неверно для коллекции с независимыми обходами.", "Не полагайтесь на индексный доступ у любого итерируемого объекта."] },
        { h: "Как отвечать на собеседовании", b: ["Протокол <code>iter</code>/<code>next</code> → состояние и одноразовость → <code>StopIteration</code> → генератор или класс как выбор реализации."] }
      ]
    },
    {
      t: "Как GIL влияет на многопоточность в CPython?", l: "Senior", c: "concurrency", g: ["python", "gil", "concurrency"],
      d: "GIL не позволяет нескольким потокам одновременно исполнять байткод Python в одном процессе CPython. Потоки полезны для ввода-вывода, а CPU задачи требуют процессов или кода, освобождающего блокировку.",
      s: [
        { h: "Короткий ответ", p: ["GIL — глобальная блокировка интерпретатора CPython. Она ограничивает одновременное выполнение байткода Python потоками одного процесса, но не мешает полезной параллельности ввода-вывода."] },
        { h: "Как это работает подробнее", p: ["В CPython поток перед выполнением очередного участка байткода должен владеть глобальной блокировкой интерпретатора. Это упрощает управление внутренними ссылочными счётчиками и некоторыми структурами объектов, но ограничивает CPU-параллелизм: два потока одного процесса не исполняют байткод Python одновременно. Планировщик периодически передаёт владение другому потоку, поэтому программа может чередовать выполнение, хотя это не равно параллельному использованию нескольких ядер. Во время блокирующего ввода-вывода стандартная библиотека и расширения могут освободить GIL. Тогда другой поток продолжает работу, пока первый ждёт диск, сеть или внешний ресурс. Для вычислений чаще применяют <code>multiprocessing</code> или <code>ProcessPoolExecutor</code>: процессы имеют отдельные интерпретаторы и память, но требуют сериализации данных и межпроцессного обмена. Нативное расширение тоже может освободить блокировку на длительной операции. GIL не является заменой синхронизации: общие бизнес-данные всё равно требуют корректной защиты от гонок. Детали зависят от реализации и версии Python, поэтому утверждение нужно ограничивать CPython и конкретным сценарием нагрузки."] , b: ["<strong>Выбор:</strong> потоки подходят для ожидания ввода-вывода, процессы — для CPU-нагрузки, а GIL не защищает произвольное состояние приложения от гонок."] },
        { h: "Пример кода", code: { lang: "python", title: "gil_demo.py", lines: ["from concurrent.futures import ThreadPoolExecutor", "from urllib.request import urlopen", "", "def read_size(url):", "    with urlopen(url) as response:", "        return len(response.read())", "", "urls = [\"https://example.com\", \"https://www.python.org\"]", "with ThreadPoolExecutor(max_workers=2) as executor:", "    for size in executor.map(read_size, urls):", "        print(size)"] } },
        { h: "Что использовать на практике", b: ["Для сетевых и файловых ожиданий используйте потоки или асинхронность. Для CPU-интенсивной работы рассматривайте процессы и измеряйте стоимость обмена данными."] },
        { h: "Подводные камни", b: ["<mark>GIL не делает код приложения потокобезопасным:</mark> составные операции над общим состоянием всё равно могут конфликтовать.", "Не переносите выводы о CPython на другие реализации Python."] },
        { h: "Как отвечать на собеседовании", b: ["GIL ограничивает байткод CPython → ввод-вывод может освобождать блокировку → процессы дают CPU-параллелизм → общие данные требуют синхронизации."] }
      ]
    },
    {
      t: "Почему сборка мусора нужна в Python?", l: "Senior", c: "memory", g: ["python", "memory", "garbage-collection"],
      d: "Python освобождает память прежде всего подсчётом ссылок, а сборщик циклического мусора ищет недостижимые циклы. Управление памятью скрыто от прикладного кода, но ресурсы лучше закрывать явно.",
      s: [
        { h: "Короткий ответ", p: ["CPython использует подсчёт ссылок и дополнительный сборщик циклов. Когда объект недостижим, его память может быть освобождена, но внешние ресурсы нужно закрывать явно."] },
        { h: "Как это работает подробнее", p: ["Каждый объект CPython хранит счётчик ссылок. При создании новой ссылки счётчик увеличивается, а при удалении уменьшается. Когда он достигает нуля, объект обычно уничтожается сразу, что даёт предсказуемое освобождение памяти для простых случаев. Циклические структуры нарушают это правило: два объекта могут ссылаться друг на друга, оставаясь недостижимыми из программы, но их счётчики не становятся нулевыми. Специальный сборщик периодически анализирует контейнеры и выявляет такие циклы. Он работает поколениями, потому что недавно созданные объекты чаще оказываются короткоживущими, а долгоживущие можно проверять реже. Сборка может быть остановлена или настроена, но ручное вмешательство нужно только после измерений. Память, возвращённая менеджеру Python, не обязательно сразу возвращается операционной системе. Это объясняет, почему процесс может сохранять размер после удаления объектов. Файлы, сокеты, блокировки и транзакции не следует связывать с моментом сборки: для них нужны <code>with</code>, <code>close</code> или явное завершение. Циклы в кэшах и замыканиях могут удерживать крупные графы объектов, поэтому важно проектировать время жизни ссылок."] , b: ["<strong>Память и ресурс:</strong> сборщик управляет объектами Python, но не заменяет явное закрытие файла, сокета, блокировки или транзакции."] },
        { h: "Пример кода", code: { lang: "python", title: "gc_demo.py", lines: ["import gc", "", "class Node:", "    pass", "", "first = Node()", "second = Node()", "first.other = second", "second.other = first", "del first, second", "print(gc.collect())"] } },
        { h: "Что использовать на практике", b: ["Управляйте внешними ресурсами через контекстные менеджеры, а настройки сборщика меняйте только после профилирования памяти и подтверждения проблемы."] },
        { h: "Подводные камни", b: ["Не ждите от сборки мусора немедленного возврата памяти операционной системе.", "Циклы и кэши могут удерживать большие объекты дольше ожидаемого."] },
        { h: "Как отвечать на собеседовании", b: ["Подсчёт ссылок → циклический сборщик → поколения → память не равна внешнему ресурсу → явное закрытие ресурсов."] }
      ]
    },
    {
      t: "Что такое исключения и как их обрабатывать в Python?", l: "Junior", c: "exceptions", g: ["python", "exceptions", "error-handling"],
      d: "Исключение сообщает о невозможности продолжить обычный путь выполнения. try, except, else и finally разделяют обработку ошибки, успешный путь и обязательную очистку ресурсов.",
      s: [
        { h: "Короткий ответ", p: ["<code>try</code> содержит рискованную операцию, <code>except</code> обрабатывает ожидаемый тип ошибки, <code>else</code> выполняет успешный путь, а <code>finally</code> запускается всегда."] },
        { h: "Как это работает подробнее", p: ["При возникновении исключения Python прекращает обычное выполнение текущего блока и ищет подходящий обработчик по стеку вызовов. Сначала проверяются обработчики текущего <code>try</code>, затем вызывающая функция и так далее. Сопоставление выполняется по типу исключения, поэтому обработчики узких классов должны идти раньше общего <code>Exception</code>. Блок <code>else</code> запускается только если исключения в <code>try</code> не было, что помогает не перехватывать ошибку из последующей логики. Блок <code>finally</code> предназначен для обязательного завершения работы: он выполняется при успехе, ошибке и выходе из функции. Пустой или слишком широкий <code>except</code> скрывает дефекты и ухудшает диагностику. Хороший обработчик добавляет контекст, сохраняет исходную причину через <code>raise ... from ...</code> и переводит низкоуровневую ошибку в понятную для слоя домена. Собственные исключения образуют небольшую иерархию, которую можно ловить без анализа текстов сообщений. Обработку следует делать на границе ответственности: библиотека сообщает ошибку, сервис решает, повторять ли операцию, а интерфейс показывает безопасное сообщение. В <code>finally</code> нельзя случайно заменить исходное исключение новым, если это не предусмотрено контрактом."] , b: ["<strong>Граница обработки:</strong> ловите конкретные ошибки, добавляйте контекст и не используйте исключения как обычный путь для управления каждой веткой."] },
        { h: "Пример кода", code: { lang: "python", title: "exceptions_demo.py", lines: ["def read_number(value):", "    try:", "        number = int(value)", "    except ValueError as error:", "        raise ValueError(\"Ожидалось целое число\") from error", "    else:", "        return number", "    finally:", "        print(\"Проверка завершена\")", "", "print(read_number(\"10\"))"] } },
        { h: "Что использовать на практике", b: ["Создавайте небольшие иерархии исключений, обрабатывайте ошибки на правильном уровне и сохраняйте исходную причину при переводе между слоями."] },
        { h: "Подводные камни", b: ["Не перехватывайте <code>Exception</code> без необходимости и не оставляйте обработчик пустым.", "Ошибка в <code>finally</code> может скрыть исходную ошибку операции."] },
        { h: "Как отвечать на собеседовании", b: ["try → конкретный except → else для успешного пути → finally для очистки → контекст и цепочка исходных исключений."] }
      ]
    }
  ]
}